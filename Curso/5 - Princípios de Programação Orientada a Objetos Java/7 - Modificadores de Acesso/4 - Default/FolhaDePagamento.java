import empresa.dados.*;

public class FolhaDePagamento {

    public static void main(String[] args) {
        TestePackage tp = new TestePackage();
        // Funcionario funcionario = new Funcionario(); // Não é possível criar o objeto funcionario pois o modificador de acesso é default
    }
    
}
